import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import 'oj-c/split-menu-button';

<Meta title="SplitMenuButton" component="oj-c-split-menu-button" />

# Split Menu Button

The `oj-c-split-menu-button` component combines an action button and menu button.

## Import

```js
//To typecheck the element APIs, import as below.
import { CSplitMenuButtonElement } from 'oj-c/split-menu-button';

//For the transpiled javascript to load the element's module, import as below
import 'oj-c/split-menu-button';
```

## Usage

Here's how to implement a basic `oj-c-split-menu-button`.

<Canvas>
  <Story id="controls-splitmenubutton--basic" name="Basic" height={350} />
</Canvas>

## Props

Here are the properties for `oj-c-split-menu-button`. To try these, switch to canvas.

<ArgsTable of="oj-c-split-menu-button" />
